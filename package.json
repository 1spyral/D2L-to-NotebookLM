{
  "name": "d2l-to-notebooklm",
  "private": true,
  "scripts": {
    "dev:extension": "pnpm --filter d2l-to-notebooklm-extension dev",
    "build:extension": "pnpm --filter d2l-to-notebooklm-extension build",
    "dev:web": "pnpm --filter web dev",
    "build:web": "pnpm --filter web build",
    "build": "pnpm build:extension && pnpm build:web",
    "typecheck": "pnpm --recursive --if-present run typecheck",
    "format": "biome format . --write",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "test": "pnpm --recursive --if-present run test",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,cjs,mjs,json,css,md,html,yml,yaml}": [
      "biome check --write --no-errors-on-unmatched"
    ]
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.7",
    "husky": "^9.1.7",
    "lint-staged": "^16.3.3",
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@10.30.2",
  "engines": {
    "node": "22.x"
  }
}
